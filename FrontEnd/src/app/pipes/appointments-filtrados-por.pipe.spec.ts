import { AppointmentsFiltradosPorPipe } from './appointments-filtrados-por.pipe';

describe('AppointmentsFiltradosPorPipe', () => {
  it('create an instance', () => {
    const pipe = new AppointmentsFiltradosPorPipe();
    expect(pipe).toBeTruthy();
  });
});
